document.addEventListener("DOMContentLoaded", () => {
    // Placeholder: Populate chart with data in the future
    const chartPlaceholder = document.querySelector("#applications-chart");
    chartPlaceholder.innerHTML = "<p>Applications data will load here dynamically.</p>";
});
